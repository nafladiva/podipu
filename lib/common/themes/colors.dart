import 'package:flutter/material.dart';

class MyColor {
  static const primary = Colors.amber;
  static const primaryPink = Color(0xFFFEA1BF);
  static const background = Color(0xFF232D3F);
  static const text = Color(0xFFFDF0F0);
  static const shadow = Color(0xFF0F0F0F);
  static const dark = Color(0xFF18122B);
}
